[package]
name = "sim-types"
description = "Shared plain types for the Portunus simulation workspace. Zero heavy deps; serde on everything."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }

[lints]
workspace = true
